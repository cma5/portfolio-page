export default class {
    constructor(params:any) {
        params = params;
    }

    setTitle(title:string){
        document.title = title;
    }

    async getHtml() {
        return "";
    }
}